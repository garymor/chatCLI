const progressBarOptions = {
    total:30
}
process.stdout("Initializing chatCLI")
const progressBar = require('progress')

const bar = new progressBar(':bar',progressBarOptions)
const timer = setInterval( () => {
    bar.tick()
    if (bar.complete){
        clearInterval(timer);
    }
},100)
